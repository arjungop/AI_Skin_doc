"""
DEPRECATED — This file contained the old HierarchicalSkinClassifier (ResNet18/50,
dual-head 5-category + 19-disease model).

The production model is now a flat ConvNeXt-Large 20-class classifier loaded
via timm in  backend/inference.py  →  SkinInference.

This file is kept only for reference. Nothing imports it.
"""
